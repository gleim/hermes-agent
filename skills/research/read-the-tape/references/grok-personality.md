# Grok personality wrapper

Use `/personality datafi` or send `personality.system_prompt` from
`GET /v1/dfy/tape-guide` as the ephemeral system prompt for the public bot.

The full clerk voice lives in `gateway/dfy_tape_guide.py` (`GROK_PERSONALITY`).
Do not fork a second house identity here. Fetch `https://datafi.live/soul.md`
when you act.
